[pytest]
# READ SUMMARY: This pytest config controls backend test discovery and temporary runtime placement.
# CHANGED: Disable cache only; tests provide their own Windows-safe tmp_path fixture.
addopts = -q -p no:cacheprovider
testpaths = tests
