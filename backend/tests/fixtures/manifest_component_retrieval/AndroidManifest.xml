<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <application>
        <activity
            android:name=".OpenActivity"
            android:exported="true" />
        <service
            android:name=".ProtectedService"
            android:exported="true"
            android:permission="example.permission.BIND_SERVICE" />
        <receiver
            android:name=".InternalReceiver"
            android:exported="false" />
        <provider
            android:name=".ExportedProvider"
            android:authorities="example.provider"
            android:exported="true"
            android:readPermission="example.permission.READ"
            android:writePermission="example.permission.WRITE" />
        <activity-alias
            android:name=".OpenAlias"
            android:targetActivity=".OpenActivity"
            android:exported="true" />
    </application>
</manifest>
