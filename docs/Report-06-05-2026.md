Below is the **detailed comparison between your original thesis plan and the current MVP implementation report**.

## Overall verdict

Your MVP is **still aligned with the thesis plan**. It has not become a random repo browser or generic chatbot. The latest report says it can import source code, index supported files into line-aware chunks, detect candidate security files, generate a Security Wiki, retrieve source-code evidence plus wiki context, ask an LLM for an evidence-backed answer, show evidence cards, compare models, store manual feedback, and export project data. 

But it is **not thesis-ready yet**. The biggest gap is that several important research-quality parts are still heuristic:

1. Parser is still regex/heuristic, not tree-sitter/AST-based.
2. Discovery returns candidate files, not true architecture modules.
3. Security Wiki is Markdown prompt output, not a validated structured schema.
4. Horizontal/vertical access-control analysis is mostly LLM-generated, not computed through static analysis.
5. Large repo handling is guarded but not robust.
6. Frontend/E2E testing is missing or weak.
7. Android is supported only as selected source/case-study style, not full AOSP. full AOSP is not needed for this MVP.

Your thesis plan expected a local Security CodeWiki that imports OSS/ZIP/selected Android source, parses line-aware chunks, discovers security modules, generates a Security Wiki, retrieves evidence before LLM answering, shows file path/line/code snippets, compares models, stores human evaluation, and exports reports.  The MVP currently implements many of these, but several are partial.

---

# 1. Original thesis purpose vs current MVP

## Original plan

The thesis problem was:

> Security auditors need a way to quickly and reliably understand where and how access control is implemented inside large OSS projects.

The original plan says normal LLM chat is not enough because it can hallucinate file paths or unsupported explanations, so the system must combine repository ingestion, file browsing, security module discovery, RAG evidence retrieval, LLM explanation, exact file/function/line evidence, code snippets, human verification, and model comparison. 

## Current MVP

Current implementation does follow that direction. It now has:

* repo/ZIP/source import,
* file browsing,
* code chunking with line ranges,
* security candidate discovery,
* Chroma indexing,
* Security Wiki generation,
* evidence-backed chat,
* model provider support,
* comparison,
* manual feedback,
* export.

The report explicitly says the MVP has not drifted into a generic chatbot because chat refuses to answer without source-code evidence and evidence cards are generated from backend chunks rather than invented by the model. 

## Comparison judgment

**Status: Mostly aligned.**

The core thesis identity is preserved.

The current system is already a working **evidence-backed audit assistant MVP**, but not yet a mature **security program comprehension research tool**.

---

# 2. Original final MVP definition vs current implementation

Your original final MVP definition required:

1. Superuser login from `.env`
2. Public GitHub repo import
3. ZIP upload
4. Android selected case-study import
5. File tree/source viewer
6. Method-level parsing with line ranges
7. RAG indexing
8. Security module discovery
9. Security Wiki generation
10. Evidence-backed chat
11. Code snippets in chat
12. Open evidence in file viewer
13. Multi-model LLM provider support
14. Human verification scoring
15. Exportable audit report 

## Current status against that definition

| MVP requirement                           | Current status | Judgment                                                                         |
| ----------------------------------------- | -------------: | -------------------------------------------------------------------------------- |
| Superuser login from `.env`               |           Done | Matches plan                                                                     |
| GitHub repo import                        |           Done | Matches plan, but large repos still risky                                        |
| ZIP upload                                |           Done | Matches plan, but indexing is not durable-job based                              |
| Android selected source/case-study import |        Partial | Case-study/source-link exists, but curated Android package workflow needs polish |
| File tree/source viewer                   |           Done | Strongly aligned                                                                 |
| Method-level parsing with line ranges     |        Partial | Line ranges exist, but parser is heuristic                                       |
| RAG indexing                              |   Done/partial | Chroma exists; semantic embeddings depend on local model availability            |
| Security module discovery                 |        Partial | Finds candidate files, not true modules                                          |
| Security Wiki generation                  |   Done/partial | Generates Markdown, not structured validated wiki                                |
| Evidence-backed chat                      |           Done | Strongly aligned                                                                 |
| Code snippets in chat                     |           Done | Evidence cards contain backend snippets                                          |
| Open evidence in file viewer              |   Done/partial | Opens and highlights, but should be tested carefully                             |
| Multi-model support                       |           Done | Ollama/Qwen, Gemini, OpenAI, DeepSeek exist                                      |
| Human verification scoring                |   Done/partial | Stored as audit/evaluation feedback; not self-training                           |
| Exportable audit report                   |        Partial | Exports exist, but not thesis-polished                                           |

## Comparison judgment

**You have achieved the MVP skeleton.**

What remains is not “start over.” It is **make the MVP reliable, structured, testable, and thesis-defensible**.

---

# 3. Repository import and project ingestion

## Original plan

The original methodology required three import modes:

1. Public GitHub repository URL
2. ZIP upload
3. Android Source / Case Study Mode

The plan also explicitly said full Android is too large and MVP should use selected case studies such as `AccountManagerService`, `ServiceManager`, or Binder token handling. 

## Current MVP

The current report says GitHub clone, ZIP upload, and Android/source-link flows exist. GitHub uses shallow clone. ZIP extraction is supported. Android is selected-source/case-study oriented, not full AOSP. Subfolder import is partially supported by shallow-cloning the repo and indexing only the selected subfolder, but it is not true sparse checkout. 

## Comparison

| Feature          | Original plan                     | Current implementation              | Gap                                       |
| ---------------- | --------------------------------- | ----------------------------------- | ----------------------------------------- |
| GitHub clone     | Shallow clone                     | Implemented                         | Good                                      |
| ZIP upload       | Supported                         | Implemented                         | Good                                      |
| Android mode     | Selected case studies             | Partial/source-link style           | Needs curated Android packages            |
| Full AOSP        | Not for MVP                       | Not supported                       | Correct decision                          |
| Subfolder import | Later/implied by large repo needs | Partial index-only subfolder        | Improve with sparse checkout if practical |
| Clone progress   | Background/progress expected      | Clone percent unavailable           | Need better UX/status                     |
| Storage          | `/storage/projects/...`           | Exists, but path confusion possible | Use absolute paths in `.env`              |

## Judgment

**Mostly aligned.**

The biggest weakness is not import itself. It is **large repo UX**: no real sparse checkout, no clone percentage, no durable queue/cancel/retry.

For thesis demo, this is acceptable if you use **small repos + selected Android packages**.

---

# 4. File browser and code viewer

## Original plan

The plan required a file browser with:

* left panel file tree,
* center code viewer,
* right panel security/chat,
* syntax highlighting,
* line numbers,
* jump/open referenced chunk,
* evidence block highlighting. 

## Current MVP

The current report says React + Monaco code viewer exists, file tree exists, file content route exists, evidence cards open the file, scroll/highlight the evidence range, and highlight critical lines. 

## Comparison

| Feature                         | Current status | Judgment                           |
| ------------------------------- | -------------: | ---------------------------------- |
| File tree                       |           Done | Good                               |
| Monaco code viewer              |           Done | Good                               |
| Line numbers                    |           Done | Good                               |
| Open file by path               |           Done | Good                               |
| Evidence card opens source file |           Done | Strong alignment                   |
| Evidence line highlighting      |   Done/partial | Good, but needs frontend/E2E tests |
| Large file tree pagination      |        Missing | Important later                    |

## Judgment

**This part is one of the strongest areas of the MVP.**

It directly supports your thesis goal because auditors can verify the answer against actual source code.

---

# 5. Parsing and chunking

## Original plan

This was one of the most important parts of the architecture. The plan said each source file should be parsed into chunks with:

* file path,
* language,
* class name,
* method/function name,
* start line,
* end line,
* method body,
* comments,
* imports.

The plan explicitly selected **tree-sitter** for MVP because it supports multiple languages, gives start/end positions, extracts functions/methods, and works without compiling the project. 

## Current MVP

The current report says the parser is regex/heuristic-based. Tree-sitter dependencies exist, but the parser does not use tree-sitter. It supports Java/C/C++ style symbols, Go functions, Python class/def/async def, JavaScript/TypeScript functions/classes/arrows/routes/decorators, Markdown headings, and fallback 80-line chunks. The report itself says this is good enough for MVP demos but not thesis-perfect for complex syntax. 

## Comparison

| Language/format | Original plan                                    | Current MVP            | Gap                   |
| --------------- | ------------------------------------------------ | ---------------------- | --------------------- |
| Java            | tree-sitter preferred                            | Regex heuristic        | Needs AST/tree-sitter |
| Go              | tree-sitter preferred                            | Function regex         | Needs AST/tree-sitter |
| C/C++           | later support                                    | C-style regex/fallback | Partial               |
| Python          | not in original first file list, but now desired | Added heuristic        | Useful but not AST    |
| JS/TS           | not original first target, now desired           | Added heuristic        | Useful but not AST    |
| Markdown/config | fallback acceptable                              | Implemented            | Fine                  |
| Line ranges     | backend-generated                                | Implemented            | Good                  |
| Function names  | parser-derived                                   | Partial reliability    | Needs stronger parser |

## Judgment

**This is the biggest technical debt.**

The MVP satisfies the visible requirement of “line-aware chunks,” but not the planned **tree-sitter-level reliability**. Since your thesis depends heavily on evidence line ranges and function-level snippets, this must be improved before final evaluation.

Priority: **Critical.**

---

# 6. File type and language support

## Original plan

The original file list included:

```text
.java, .go, .cpp, .c, .h, .aidl, .xml, .json, .yaml, .yml, .te, .md, .txt
```

The initial focus was Java/Go, then later C++. 

## Current MVP

Current implementation now supports more than the original list:

* Java/Spring
* Android Java
* AIDL
* C/C++
* SELinux `.te`
* Go/Kubernetes Go
* Python/FastAPI
* JavaScript/TypeScript/Node/Express
* JSON/YAML/XML
* Markdown/TXT

However, the report says most non-config parsers are heuristic and not full AST. 

## Comparison

| Area                          | Status                                      |
| ----------------------------- | ------------------------------------------- |
| Java/Spring                   | Better than original scope, but parser weak |
| Android Java/AIDL/C++/SELinux | Aligned with thesis target, but shallow     |
| Go/Kubernetes                 | Aligned, but no Kubernetes semantic model   |
| Python/FastAPI                | Added after your issue; good expansion      |
| JS/TS/Node                    | Added; useful but heuristic                 |
| Config files                  | Good enough for search/RAG                  |

## Judgment

**Language coverage is now broader than the thesis plan, but parser depth is weaker than the thesis plan.**

This means the MVP can demo many repo types, but final claims must be careful:

> “The system indexes and heuristically chunks multiple languages.”

Do not claim:

> “The system deeply understands all languages.”

---

# 7. Security candidate detection

## Original plan

The plan required cheap pre-LLM security candidate detection with keywords such as:

* permission,
* authorize,
* authentication,
* access,
* role,
* policy,
* SecurityException,
* checkPermission,
* enforcePermission,
* getCallingUid,
* hasSignatureCapability,
* Binder,
* SELinux,
* RBAC,
* SubjectAccessReview.

Chunks should be tagged as:

* `potential_entry_point`,
* `potential_access_check`,
* `potential_helper`,
* `potential_policy_file`,
* `potential_config_file`.

This was intended to reduce cost and improve retrieval. 

## Current MVP

Current implementation has keyword/tag-based detection. It supports Java/Android/Spring terms, Python/FastAPI/Django terms, JS/TS/Node/Nest terms, config/policy files, and generic auth terms. 

## Comparison

| Requirement                |       Current status | Judgment              |
| -------------------------- | -------------------: | --------------------- |
| Security keyword detection |                 Done | Good                  |
| Security tags              |                 Done | Good                  |
| Multi-stack security terms | Better than original | Good                  |
| Confidence scoring         |         Done/partial | Heuristic             |
| Real security semantics    |              Missing | Expected at MVP level |

## Judgment

**Aligned and expanded.**

But it is still keyword-based. That is okay for discovery, not enough for final security conclusions.

Use wording like:

> “The detector identifies candidate security-relevant files; it does not prove that access control is correct.”

---

# 8. Discovery / “module” selection

## Original plan

The user may not know the target module, so the system should support:

* known target workflow,
* audit-goal workflow,
* target discovery using keyword search, symbol search, vector search, security tags, optional LLM ranking,
* then show candidate modules such as `AccountManagerService.java`, `IAccountManager.aidl`, `PackageManagerInternal.java`. 

## Current MVP

Discovery returns candidate files/modules using backend scoring, not LLM. It uses file path, chunk tags, symbol names, audit-goal terms, and chunk counts. The selected “module” is currently a file path, not a true module entity. The report explicitly says discovery returns candidate files rather than real architecture modules. 

## Comparison

| Feature                            | Original plan                 | Current MVP              | Gap                                |
| ---------------------------------- | ----------------------------- | ------------------------ | ---------------------------------- |
| User audit goal                    | Planned                       | Implemented              | Good                               |
| Keyword/symbol/security tag search | Planned                       | Implemented              | Good                               |
| Vector search                      | Planned                       | Some retrieval exists    | Needs clarity in discovery ranking |
| Optional LLM ranking               | Optional                      | Not implemented          | Not critical                       |
| Candidate modules                  | Planned                       | Candidate files          | Conceptual gap                     |
| Select target                      | Planned                       | Implemented as file path | Acceptable MVP                     |
| Real module entity                 | Not strictly MVP, but implied | Missing                  | Later                              |

## Judgment

**Functionally acceptable, conceptually weak.**

For MVP, candidate file = selected module is okay. But for thesis language, say:

> “Candidate security file/module.”

Do not oversell it as architectural module discovery.

---

# 9. RAG and vector indexing

## Original plan

The plan said after chunking, each chunk should be embedded into ChromaDB. Metadata should be stored in SQLite. Embedding text should include metadata plus code, such as file, class, method, line range, tags, and code. 

## Current MVP

The current report says ChromaDB is used. Raw code chunks and wiki chunks are indexed. Embeddings default to sentence-transformers with hash fallback. Code vector metadata includes `source_type`, chunk ID, project ID, file path, symbol, line range, language, and tags. Wiki chunks have separate metadata. 

## Comparison

| Requirement          | Current status | Judgment                              |
| -------------------- | -------------: | ------------------------------------- |
| ChromaDB             |           Done | Good                                  |
| SQLite metadata      |           Done | Good                                  |
| Code chunks embedded |           Done | Good                                  |
| Metadata included    |           Done | Good                                  |
| Wiki chunks embedded |           Done | Good; originally needed               |
| Semantic embeddings  |   Done/partial | Depends on local model availability   |
| Hash fallback        |         Exists | Useful dev fallback, not thesis-grade |
| Retrieval quality    |        Partial | Needs evaluation                      |

## Judgment

**Architecture aligned. Retrieval quality still needs proof.**

The biggest practical issue: if sentence-transformers model is not available locally, system may fall back to hash embeddings. That is not thesis-grade. For final testing, you must confirm semantic embedding is active.

---

# 10. Security Wiki generation

## Original plan

The Security Wiki was supposed to be your own free wiki, not Google CodeWiki. It should be security-focused, not a general project wiki. Planned pages included:

1. Module Security Overview
2. Public Entry Points
3. Horizontal Access-Control Matrix
4. Vertical Helper Analysis
5. Requirement-to-Code Traces
6. Evidence Blocks
7. Human Auditor Notes

The plan also said the LLM should preferably return structured JSON and the app should render Markdown/HTML from that structured output. 

## Current MVP

Current system generates its own Security Wiki. Google CodeWiki is not used. Wiki is generated for selected candidate file/module, stored in SQLite, indexed into Chroma, and used as chat context. However, output is Markdown prompt output, not validated structured JSON. 

## Comparison

| Feature              | Original plan     | Current MVP         | Gap                      |
| -------------------- | ----------------- | ------------------- | ------------------------ |
| Own Security Wiki    | Planned           | Implemented         | Good                     |
| Not Google CodeWiki  | Planned           | Implemented         | Good                     |
| Target-specific wiki | Planned           | Implemented         | Good                     |
| Security sections    | Planned           | Prompted Markdown   | Good but inconsistent    |
| Structured JSON      | Preferred/planned | Missing             | Important                |
| Evidence refs        | Planned           | Partial             | Need chunk-ID validation |
| Indexed into RAG     | Planned           | Done                | Good                     |
| Used in chat         | Planned           | Done as orientation | Good                     |

## Judgment

**Strong direction, weak structure.**

Security Wiki exists, but it needs a schema.

Priority: **High/Critical** before thesis writing, because this is one of your named contributions.

---

# 11. Evidence-backed chat

## Original plan

Chat should not send the question directly to the LLM. It should:

1. understand query,
2. retrieve source chunks + Security Wiki chunks,
3. build evidence package,
4. send evidence to selected LLM,
5. generate grounded answer,
6. show code snippet and file link. 

The plan also said line numbers must come from the backend, not the LLM, and if evidence is insufficient, the model should say the answer is not verified. 

## Current MVP

Current chat retrieves raw source evidence and wiki context, refuses no-evidence questions, sends separated source evidence and wiki context to the LLM, asks for structured JSON, parses with Pydantic, validates evidence refs, and falls back if invalid JSON. Evidence snippets come from stored backend chunks, not the LLM. 

## Comparison

| Feature                          | Current status | Judgment                            |
| -------------------------------- | -------------: | ----------------------------------- |
| Evidence first                   |           Done | Excellent                           |
| No evidence, no answer           |           Done | Excellent                           |
| Code evidence + wiki context     |           Done | Good                                |
| Backend-generated evidence cards |           Done | Excellent                           |
| Structured JSON answer           |   Done/partial | Good but strengthen fallback/repair |
| Hallucination control            |        Partial | Reduced but not eliminated          |
| Query understanding              |          Basic | Could improve                       |
| Helper chain support             |      LLM-based | Needs call graph later              |

## Judgment

**This is the most thesis-aligned part of the MVP.**

The system now demonstrates your central research concept.

Remaining issue: improve JSON validation and ensure prose cannot make unsupported claims.

---

# 12. Evidence cards and exact code location

## Original plan

The answer should show:

* exact file path,
* function/method name,
* line range,
* code snippet,
* explanation,
* helper chain where available,
* confidence/limitations,
* open in file viewer button. 

The line range must come from backend parser, not the LLM. 

## Current MVP

Evidence cards include chunk ID, file path, symbol, class, line range, language, tags, snippet, critical lines, and clicking opens source in Monaco with range/critical-line highlighting. 

## Comparison

| Feature          |   Current status | Judgment              |
| ---------------- | ---------------: | --------------------- |
| File path        |             Done | Good                  |
| Symbol/block     |     Done/partial | Depends on parser     |
| Line range       |             Done | Good                  |
| Code snippet     |             Done | Good                  |
| Critical lines   |   Done/heuristic | Useful                |
| Open file viewer |             Done | Good                  |
| Highlight range  |     Done/partial | Needs UI tests        |
| Exactness        | Parser-dependent | Needs AST/tree-sitter |

## Judgment

**Feature implemented well, but precision depends on parser quality.**

This loops back to the tree-sitter gap.

---

# 13. LLM provider layer and model comparison

## Original plan

The architecture required a provider-agnostic LLM layer with:

* Ollama/Qwen,
* Gemini,
* OpenAI,
* DeepSeek,

and model comparison using the same evidence package. 

## Current MVP

Current report says Ollama/Qwen, Gemini, OpenAI, DeepSeek are implemented. Model health exists. Model comparison retrieves evidence once and sends same evidence to selected providers. It persists results and has manual scoring fields. 

## Comparison

| Feature                 | Current status | Judgment               |
| ----------------------- | -------------: | ---------------------- |
| Provider abstraction    |           Done | Good                   |
| Ollama/Qwen             |           Done | Good                   |
| Gemini                  |   Done/partial | Default model nuance   |
| OpenAI                  |           Done | Good                   |
| DeepSeek                |           Done | Good                   |
| Model health checks     |           Done | Good                   |
| Same evidence to models |           Done | Good                   |
| Manual scoring          |   Done/partial | Good basis             |
| Evaluation dashboard    |        Partial | Needs polish           |
| Cost estimation         |           Weak | Not critical initially |

## Gemini issue

The current report says Gemini default is `gemini-1.5-flash`, but if the env value is blank, `resolved_gemini_default_model` falls back to `gemini-2.5-flash`. This is nuanced and can confuse you because you only added a free API key. 

## Judgment

**Model comparison is ahead of basic MVP expectations, but not yet research-polished.**

Next step should be a clear model config UI/status and better comparison export.

---

# 14. Human feedback / evaluation

## Original plan

The MVP should allow human auditor scoring: correct, incomplete, wrong, or needs review. The plan emphasized human verification remains necessary. 

## Current MVP

Manual verification exists. Model comparison scoring exists. The report clearly says feedback is manual audit/evaluation data only and does not self-train the model. 

## Comparison

| Requirement                | Current status | Judgment                       |
| -------------------------- | -------------: | ------------------------------ |
| Verification labels        |           Done | Good                           |
| Evaluation scoring         |   Done/partial | Good                           |
| Self-training avoided      |           Done | Correct                        |
| Export feedback            |        Partial | Needs better report formatting |
| Expert evaluation workflow |        Partial | Needs final dataset/questions  |

## Judgment

**Aligned.**

Do not implement self-training. Your system is for **evaluation and audit feedback**, not learning from users.

---

# 15. Export/report generation

## Original plan

Exportable audit report was part of final MVP, with Markdown first, PDF later, CSV for access-control matrix. 

## Current MVP

Exports exist in Markdown, JSON, CSV. They include project metadata, wiki pages, chat Q/A, evidence JSON, evaluations, verifications, and limitations. But the report says export is functional, not polished thesis artifact. 

## Comparison

| Export feature               | Current status | Gap                             |
| ---------------------------- | -------------: | ------------------------------- |
| Markdown export              |           Done | Needs better structure          |
| JSON export                  |           Done | Good for data                   |
| CSV export                   |           Done | Needs cleaner evaluation tables |
| PDF export                   |        Missing | Was later, not urgent           |
| Thesis report format         |        Partial | Needs polish                    |
| Access-control matrix export |        Partial | Depends on wiki/chat output     |

## Judgment

**MVP export exists, but supervisor-facing report quality is not ready.**

This is not blocking development, but it matters before thesis demo.

---

# 16. Large repository and Android handling

## Original plan

The plan explicitly said Android full source is too large and MVP should use selected submodules/case studies. It also listed challenges: large size, slow clone, storage, parsing complexity, LLM cost, version changes. 

## Current MVP

Current implementation has shallow clone, file/chunk limits, repo size guardrails, subfolder index-only support, timeouts, and progress fields. Full AOSP is explicitly not supported. Clone percentage is not available. Subfolder import is not true sparse checkout. 

## Comparison

| Feature                   | Current status | Judgment                |
| ------------------------- | -------------: | ----------------------- |
| Full AOSP not attempted   |        Correct | Good practical choice   |
| Selected Android source   |        Partial | Needs curated samples   |
| Repo size limits          |           Done | Good                    |
| Subfolder import          |        Partial | Useful but not sparse   |
| Clone timeout             |           Done | Good                    |
| Clone percentage          |        Missing | Acceptable but UX issue |
| Durable job queue         |        Missing | Later                   |
| Cancel/retry              |        Missing | Later                   |
| Android-specific analysis |        Partial | Need selected demos     |

## Judgment

**The MVP handles large repos realistically but not comfortably.**

For thesis, do **not** try full AOSP. Use selected Android packages.

---

# 17. Testing

## Original plan

Testing was expected around import, chunking, retrieval, wiki, chat, evidence cards, model comparison, and export. The prior test plan included backend, frontend, and integration tests. 

## Current MVP

Backend tests exist. They cover schema, ZIP safety, parser/chunker, security detection, file filtering, Python/JS parsing, no-evidence chat, evidence integrity, wiki storage/indexing, verification, model comparison, Gemini default behavior, subfolder import, and missing subfolder failure. No frontend test framework is configured. 

## Comparison

| Test area                   | Current status | Gap                    |
| --------------------------- | -------------: | ---------------------- |
| Backend tests               |           Good | Keep expanding         |
| Parser tests                |        Present | Need tree-sitter later |
| No-evidence chat            |        Present | Good                   |
| Wiki indexing tests         |        Present | Good                   |
| Model comparison tests      |        Present | Good                   |
| Frontend tests              |        Missing | Important              |
| E2E tests                   |        Missing | Important              |
| Evidence highlight UI tests |        Missing | Important              |

## Judgment

**Backend testing is now decent. Frontend/E2E is the gap.**

Before demo, add at least Playwright smoke tests for import → discovery → wiki → chat → evidence click.

---

# 18. Mapping to professor’s methodology

Your professor’s report and your plan focused on:

1. Horizontal access-control analysis
2. Vertical helper analysis
3. Requirement-to-code tracing
4. Permission/policy mapping
5. Evidence-backed audit artifacts
6. Human-in-the-loop review

## Current implementation vs methodology

| Method                         | Current implementation                               | Status       |
| ------------------------------ | ---------------------------------------------------- | ------------ |
| Horizontal AC matrix           | LLM can generate from retrieved snippets/wiki        | Partial      |
| Vertical helper analysis       | LLM can infer if helper chunks retrieved             | Partial      |
| Requirement-to-code trace      | User can ask; evidence retrieval supports answers    | Partial      |
| Permission/policy mapping      | Keyword detection across Android/Spring/FastAPI/Node | Partial      |
| Evidence-backed audit artifact | Wiki + exports exist                                 | Partial/good |
| Human review                   | Verification + model scoring                         | Good         |

The latest report says horizontal matrix and helper chains are still not computed by static analysis; they are LLM-generated from retrieved snippets. The earlier gap analysis also stated there is no real call graph support and helper chains are inferred from retrieved evidence. 

## Judgment

**The MVP demonstrates the methodology, but does not fully implement it scientifically yet.**

For the thesis, you can say:

> “This MVP operationalizes LLM-assisted horizontal/vertical access-control comprehension using evidence retrieval, but static call-graph validation remains future work.”

That is honest and defensible.

---

# 19. What has been achieved

You have achieved a lot.

## Achieved from thesis plan

* Local web MVP
* React frontend
* FastAPI backend
* SQLite
* ChromaDB
* Superuser login
* GitHub import
* ZIP import
* Android source/case-study style import
* File browser
* Monaco code viewer
* Line-aware chunks
* Security keyword detection
* Candidate discovery
* Security Wiki generation
* Wiki storage
* Wiki indexing into Chroma
* Evidence-first chat
* No evidence refusal
* Evidence cards
* Evidence opens in code viewer
* Critical-line detection
* Ollama/Qwen
* Gemini
* OpenAI
* DeepSeek
* Model health
* Model comparison
* Manual feedback
* Export
* Backend tests

This matches the current implementation summary, which describes the system as a working local MVP with import, indexing, candidate security detection, wiki generation, source evidence retrieval, LLM answers, evidence cards, model comparison, feedback, and export. 

---

# 20. What is missing or weak

## Critical

### 1. Tree-sitter/AST parsing

Original plan explicitly selected tree-sitter. Current code uses regex/heuristics. This affects line precision, function detection, helper chains, and audit reliability. 

### 2. Structured Security Wiki schema

Original plan wanted generated JSON/rendered Markdown. Current wiki is Markdown prompt output. It is usable but not consistent enough for evaluation. 

### 3. Retrieval quality validation

RAG exists, but retrieval is not yet deeply evaluated. Since “no evidence, no answer” depends on retrieval quality, this must be tested.

## High

### 4. Real module model

Current “module” is a selected file path. That is okay for MVP but weak conceptually.

### 5. Large repo handling

Current guardrails exist but no durable job queue/cancel/retry/sparse checkout.

### 6. Android case-study polish

Android support exists, but you need curated Android sample packages for demo.

### 7. Frontend/E2E tests

Evidence card opening and highlighting is thesis-critical; test it.

## Medium

### 8. Export polish

Functional export exists, but not thesis/demo-polished.

### 9. Model comparison dashboard

Data exists, UI/reporting needs improvement.

### 10. Cost estimation

Not important for core MVP, but useful for thesis evaluation.

---

# 21. What is implemented wrongly or weakly

## 1. “Modules” are actually files

The API and UI still use module wording, but current selected module is a file path. This is acceptable only if labeled clearly as **candidate security file/module**.

## 2. Parser is not what was planned

The plan said tree-sitter. Current implementation uses heuristics. This is the largest architectural mismatch. 

## 3. Security Wiki is not structured enough

It should be schema-driven; currently it is Markdown prompt output. The report itself identifies this as weak. 

## 4. Android is not a real package workflow yet

The plan wanted selected Android packages/case studies. Current Android mode is source-link/case-study style, but not polished enough.

## 5. Subfolder clone is not sparse checkout

Current behavior: shallow clone entire repo, then index selected subfolder. For large repos, this still downloads more than necessary.

## 6. Model comparison is useful but basic

The core flow exists, but the research dashboard/export needs polish.

## 7. Export is not thesis-report quality

Good for data, not yet ideal for supervisor presentation.

---

# 22. Recommended next implementation order

Do **not** randomly add more features. The MVP already has many moving parts. Focus on making it reliable.

## Phase 1 — Parser reliability

Goal: make evidence ranges more trustworthy.

Tasks:

* implement tree-sitter for Java first,
* add Python AST or tree-sitter Python,
* add Go tree-sitter,
* keep regex fallback,
* add tests for multiline methods/classes/decorators.

Why first?

Because your whole thesis depends on file path + function + line range evidence.

## Phase 2 — Structured Security Wiki

Goal: turn wiki into a reliable audit artifact.

Tasks:

* define `SecurityWiki` Pydantic schema,
* LLM returns JSON,
* validate evidence refs by chunk ID,
* render Markdown from validated JSON,
* fallback safely if invalid.

## Phase 3 — Retrieval quality

Goal: improve evidence returned to LLM.

Tasks:

* show retrieval score/reason,
* combine vector + keyword + symbol + security tag scoring transparently,
* test retrieval with known questions,
* add expected-evidence fixtures.

## Phase 4 — Android demo package

Goal: make Android thesis demo repeatable.

Tasks:

* create curated ZIP/sample package for AccountManagerService,
* include related AIDL/helper/config files,
* add Android-specific demo questions,
* export sample report.

## Phase 5 — Evaluation polish

Goal: support thesis results chapter.

Tasks:

* model comparison dashboard,
* scoring summaries,
* export comparison table,
* 10–20 prepared questions,
* expert scoring workflow.

## Phase 6 — Frontend/E2E tests

Goal: avoid demo failure.

Tasks:

* Playwright smoke test:
  import repo → discovery → select candidate → generate wiki → ask chat → open evidence → export.

---

# 23. Final side-by-side summary

| Area                | Alignment level | Short reason                               |
| ------------------- | --------------: | ------------------------------------------ |
| Thesis identity     |          Strong | Still evidence-backed Security CodeWiki    |
| Local web MVP       |          Strong | React + FastAPI works                      |
| Repo/ZIP import     |          Strong | Implemented                                |
| Android strategy    |         Partial | selected-source style only                 |
| File browser        |          Strong | Monaco viewer works                        |
| Line-aware chunks   |          Medium | line ranges exist but parser weak          |
| Tree-sitter         |         Missing | original plan not met                      |
| Security detection  |   Strong/medium | broad keyword support                      |
| Discovery           |          Medium | candidate files, not true modules          |
| RAG                 |   Strong/medium | Chroma and wiki chunks exist               |
| Evidence-first chat |          Strong | core thesis goal works                     |
| Security Wiki       |          Medium | generated/indexed but not schema-validated |
| Model providers     |          Strong | local + cloud providers exist              |
| Model comparison    |          Medium | works but needs dashboard/export polish    |
| Manual feedback     |   Strong/medium | implemented as evaluation only             |
| Export              |          Medium | works but not polished                     |
| Large repo handling |     Medium/weak | guardrails but no durable job/sparse clone |
| Testing             |          Medium | backend tests good, frontend/E2E missing   |

---

# Final conclusion

Your MVP is **about 65–75% aligned with the thesis plan** depending on how strict you are.

For a demo, it is already meaningful.

For thesis submission, it needs these three things most urgently:

1. **Stronger parser/tree-sitter or AST-based chunking**
2. **Structured validated Security Wiki schema**
3. **Better evaluation/export/testing**

The most important thing: **do not expand the project too much now.** You already have the skeleton. The work now is to make the existing pipeline robust, testable, and academically defensible.
